name: Run tests

on:  
  push:
  pull_request:
    branches:
      - main

jobs:  
  test:
    runs-on: ubuntu-latest

    if: ${{ !contains(github.event.head_commit.message, '#docs') }}
    steps:
      - name: Run tests
        run: echo running tests
